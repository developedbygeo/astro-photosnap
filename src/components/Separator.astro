---
import { cn } from "@/lib/utils";
import type { WithClassName } from "@/types/UI";

interface Props extends WithClassName {}

const { className } = Astro.props;
---

<div class={cn("w-full h-px bg-[#979797]", className)}></div>
